import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 142,
    alignItems: 'center',
  },

  container__header: {
    alignItems: 'center',
    marginBottom: 230,
  },

  header_logo: {
    width: 112,
    height: 46,
    marginBottom: 7,
  },
  'header__hading-container': {
    width: 325,
    height: 90,
  },
});
