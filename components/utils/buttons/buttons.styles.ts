import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  'border-line': {
    justifyContent: 'center',
    alignItems: 'center',
    padding: 6,
    borderRadius: 10,
    borderColor: '#6565FF',
    borderWidth: 1,
    width: 375,
    height: 48,
  },

  'border-line__text': {
    color: '#3434FF',
    fontSize: 16,
    fontWeight: 700,
  },
});
